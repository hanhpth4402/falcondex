import {ModalBody} from "react-bootstrap";

export const AddLiquidityConfirmationModal: React.FC<{
    attemptingTxn: boolean,
    onAdd: () => void,
    txHash: string,
}> = () => {

    return (
        <ModalBody>

        </ModalBody>
    )
}
